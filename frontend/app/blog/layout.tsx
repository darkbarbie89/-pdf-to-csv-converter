export const metadata = {
  title: 'Blog | PDF2CSV',
  description: 'Articles and insights on PDF tools, AI extraction, and productivity.',
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
